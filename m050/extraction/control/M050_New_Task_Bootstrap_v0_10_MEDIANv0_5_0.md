# MEDIAN successor-thread cold-start audit v0.10

Paste the following into a new Compile task. It grants no authority.

> Begin a repository-governed cold start from the current local checkout. Confirm root `AGENTS.override.md` is absent, read root `AGENTS.md` completely, and perform its required cold-start sequence exactly. Remain read-only.
>
> Report with active-path citations: Local `HEAD`, `origin/main`, and equality; the Exact corpus vector; Authorial Grammar as next source; accepted C0001/C0002 and rejected C0003 boundaries; the generic lean-table prompt and validator rule; unchanged 13-chunk plan; C0001/C0002/C0003 replay results; frozen C0003 pilot and $0.122044 ceiling; exact cumulative spend and active money-only balance; independent lifecycle and spend authority; halt conditions; prohibited later stages; next possible transition; and the Complete guard result and 121-test count.
>
> If controls conflict, hashes fail, required files are missing, or `AGENTS.override.md` exists, stop and report it. Do not modify files, contact a provider, interact with Google Sheets, or begin extraction.

No repository-write, provider-call, retry, full-source, or additional-spend
authority is granted.
