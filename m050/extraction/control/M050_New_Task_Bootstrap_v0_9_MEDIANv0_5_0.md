# MEDIAN successor-thread cold-start audit v0.9

Paste the following into a new Compile task. It grants no authority.

> Begin a repository-governed cold start from the current local checkout. Confirm root `AGENTS.override.md` is absent, read root `AGENTS.md` completely, and perform its required cold-start sequence exactly. Remain read-only.
>
> Report with active-path citations: Local `HEAD`, `origin/main`, and equality; the Exact corpus vector; Authorial Grammar as the next source; accepted new-plan C0001 and C0002 scope; rejected C0003 scope; the B00071 nonsemantic layout-metadata atom defect; revoked remaining release and uncalled C0004-C0013; exact run and cumulative spend plus active money-only balance; the independent lifecycle and spend-envelope boundary; Every condition that halts execution; prohibited later stages; the next possible author transition; and the Complete guard result and test count.
>
> If any control conflicts, hash fails, required file is missing, `AGENTS.override.md` exists, or state is ambiguous, stop and report it. Do not modify files, delete the rejected atom, retry a call, change the prompt/schema/exclusion policy, contact a provider, interact with Google Sheets, or begin extraction.

The audit grants no repository-write, calibration-change, provider-call,
retry, full-source, or additional-spend authority.
