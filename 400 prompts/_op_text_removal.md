Reproduce the image attached to this message with every piece of text removed, and nothing else changed. Whatever image is attached is the target — disregard its filename.

This stripped version is a production base. The text will be recomposited offline as editable vector type, positioned to the layout you return — so the layout must survive exactly, and every space that held text must remain open to receive it.

**REMOVE:** every word, letter, numeral, caption, label, heading, title, footer line and callout string, wherever it appears.

**PRESERVE EXACTLY — position, size and proportion unchanged:**

- Every illustration, at its current crop, scale and position. Do not re-render, re-crop, re-frame, re-light or redraw any artwork.
- Every panel, border, frame, hairline rule, divider and table cell.
- Every icon, badge, marker, leader line, arrow and pointer.
- **The now-empty header bar, and the now-empty footer bar.**
- **The now-empty heading areas above each illustration**, and any block that held body or descriptive text.
- Faint ruled lines where lines of text sat, so the layout still reads.
- The exact aspect ratio and overall dimensions of the original.

**CRITICAL — DO NOT REFLOW.** Removing text leaves gaps, and those gaps are deliberate. Do not expand, enlarge, stretch, re-centre or re-compose any illustration or panel into space vacated by removed text. Do not close up empty areas. Do not rebalance the composition. **An empty region where text used to be is the correct result, not a problem to solve.** The most common failure is an illustration growing upward into a vacated title area — do not do this.

The output should look exactly like the attached image with the words lifted cleanly off it.
