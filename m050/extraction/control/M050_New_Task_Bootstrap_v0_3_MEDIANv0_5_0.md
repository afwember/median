# MEDIAN successor-thread cold-start audit v0.3

Paste the following into a new Compile thread. It grants no authority.

> Begin a repository-governed cold start from the current local checkout. Do not rely on conversation memory, archived threads, `m051`, or retired controls.
>
> Confirm that root `AGENTS.override.md` is absent, then read root `AGENTS.md` completely and perform its required cold-start sequence exactly. Remain read-only. Do not create or modify files, authorize anything, contact a provider, interact with Google Sheets, or begin extraction.
>
> Report this acceptance audit, citing the active repository path supporting every answer:
>
> 1. Local `HEAD`, `origin/main`, and whether they are equal.
> 2. Exact corpus vector.
> 3. Approved next source and its position in the processing order.
> 4. Current Authorial Grammar lifecycle state.
> 5. What Pilot 001-R6 acceptance proves and what it does not authorize.
> 6. Remaining ordered chunk IDs and provider-call count.
> 7. Bound model, reasoning effort, and cache TTL.
> 8. Separate roles of the source-run lifecycle receipt and cumulative spend envelope.
> 9. Routine artifact policy and sequential review gate.
> 10. Every condition that halts execution.
> 11. Prohibited later-stage activities.
> 12. Exact next authorized transition.
> 13. Complete guard result and test count.
>
> If any control conflicts, hash fails, required file is missing, `AGENTS.override.md` exists, or state is ambiguous, stop and report the discrepancy rather than resolving it from memory.

The audit must be correct before Asa Wember transfers repository-write,
provider-call, full-source, or spend authority.
