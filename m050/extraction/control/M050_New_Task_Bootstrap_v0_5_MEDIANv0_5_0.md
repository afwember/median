# MEDIAN successor-thread cold-start audit v0.5

Paste the following into a new Compile task. It grants no authority.

> Begin a repository-governed cold start from the current local checkout. Do not rely on conversation memory, archived tasks, `m051`, or retired controls.
>
> Confirm that root `AGENTS.override.md` is absent, then read root `AGENTS.md` completely and perform its required cold-start sequence exactly. Remain read-only. Do not create or modify files, authorize anything, contact a provider, interact with Google Sheets, or begin extraction.
>
> Report this acceptance audit, citing the active repository path supporting every answer:
>
> 1. Local `HEAD`, `origin/main`, and whether they are equal.
> 2. Exact corpus vector and next source.
> 3. Original C0001 failure and revoked five-chunk release.
> 4. Why chunk count is a generated result rather than a manifest input.
> 5. Calibrated 20-target quantization and generated 12-chunk plan.
> 6. Quantized C0001 pilot mechanical and substantive acceptance result.
> 7. Review-gate repair and compatibility boundary.
> 8. Exact cumulative spend and remaining active money-only balance.
> 9. Separate roles of the exhausted pilot lifecycle, any future source-run lifecycle receipt, and the cumulative spend envelope.
>    In exact terms, explain the source-run lifecycle receipt and cumulative spend envelope as independent authorities.
> 10. Bound model, reasoning effort, output limit, and cache TTL.
> 11. Routine artifact policy and sequential review gate.
> 12. Every condition that halts execution.
> 13. Prohibited later-stage activities.
> 14. Exact next possible author transition.
> 15. Complete guard result and test count.
>
> If any control conflicts, hash fails, required file is missing, `AGENTS.override.md` exists, or state is ambiguous, stop and report the discrepancy rather than resolving it from memory.

The audit must be correct before Asa Wember transfers repository-write,
provider-call, full-source, or additional spend authority.
