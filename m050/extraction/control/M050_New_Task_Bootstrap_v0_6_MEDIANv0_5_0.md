# MEDIAN successor-thread cold-start audit v0.6

Paste the following into a new Compile task. It grants no authority.

> Begin a repository-governed cold start from the current local checkout. Confirm root `AGENTS.override.md` is absent, read root `AGENTS.md` completely, and perform its required cold-start sequence exactly. Remain read-only.
>
> Report with active-path citations: Local `HEAD`, `origin/main`, and equality; the Exact corpus vector; Authorial Grammar as the next source; accepted quantized C0001 scope; the C0002 semantic list-boundary defect; revoked remaining-source release; why chunk count is a generated result; exact cumulative spend and active money-only balance; the independent source-run lifecycle receipt and cumulative spend envelope; Every condition that halts execution; prohibited later stages; the next possible author transition; and the Complete guard result and test count.
>
> If any control conflicts, hash fails, required file is missing, `AGENTS.override.md` exists, or state is ambiguous, stop and report it. Do not modify files, contact a provider, interact with Google Sheets, or begin extraction.

The audit grants no repository-write, provider-call, replan, full-source, or
additional-spend authority.
