# MEDIAN successor-thread cold-start audit v0.4

Paste the following into a new Compile task. It grants no authority.

> Begin a repository-governed cold start from the current local checkout. Do not rely on conversation memory, archived tasks, `m051`, or retired controls.
>
> Confirm that root `AGENTS.override.md` is absent, then read root `AGENTS.md` completely and perform its required cold-start sequence exactly. Remain read-only. Do not create or modify files, authorize anything, contact a provider, interact with Google Sheets, or begin extraction.
>
> Report this acceptance audit, citing the active repository path supporting every answer:
>
> 1. Local `HEAD`, `origin/main`, and whether they are equal.
> 2. Exact corpus vector.
> 3. Approved next source and its position in the processing order.
> 4. Accepted R6 pilot boundary.
> 5. C0001 failure, exact spend, cache result, and revocation state.
> 6. Why chunk count is a generated result rather than a manifest input.
> 7. Calibrated target-block quantization and generated whole-source chunk sequence.
> 8. Bound model, reasoning effort, output limit, and cache TTL.
> 9. Current spend-envelope balance and inactive state.
> 9a. Separate roles of the source-run lifecycle receipt and cumulative spend envelope.
> 10. Routine artifact policy and sequential review gate.
> 11. Every condition that halts execution.
> 12. Prohibited later-stage activities.
> 13. Exact next possible author transition.
> 14. Complete guard result and test count.
>
> If any control conflicts, hash fails, required file is missing, `AGENTS.override.md` exists, or state is ambiguous, stop and report the discrepancy rather than resolving it from memory.

The audit must be correct before Asa Wember transfers repository-write,
provider-call, pilot, full-source, or spend authority.
