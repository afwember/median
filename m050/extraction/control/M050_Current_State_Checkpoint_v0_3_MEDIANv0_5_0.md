# MEDIAN v0.5.0 Current-State Checkpoint v0.3

Date: 2026-08-03

State: `AUTHORIAL_GRAMMAR_PILOT_R6_ACCEPTED_EXTRACTION_MACHINE_OFFLINE_RETOOLING`

## Corpus and source

- Corpus vector: **24 / 22 / 4 / 18 = 14 + 4**.
- Next outstanding source: Authorial Grammar.
- Pilot 001-R6 is accepted as perfect-for-release for its representative regimes.
- Pilot acceptance does not release the remaining five-chunk source plan.
- Further provider calls: zero authorized.

## Reusable extraction machine

The post-R6 implementation replaces source-specific call scripts prospectively with one controller and declarative source configurations. It renders source-bounded packets, explicitly caches the stable Claude system/schema prefix for one hour, computes cache-aware exact cost, enforces an independent cumulative money envelope, validates responses generically, and uses one append-only chained run ledger to block the next call until review passes.

For each later source, the same controller now provides a zero-call scaffold after identity-card approval. It drafts the manifest, conservative dispositions, structural chunk plan, prompt, schema, and provider-disabled configuration for review and pilot calibration; it does not create source-specific worker code or grant execution authority.

Historical R1–R6 artifacts remain immutable. New routine calls use one packet, one raw response, one outcome, and one run-ledger append rather than a new file family for each transition.

The accepted R6 structured response replays through the generic validator with zero errors. No provider call was used for the retooling.

## Authority boundary

Offline machine implementation and verification are authorized for this session. Full-source Authorial Grammar execution still requires a separate source-run lifecycle receipt. A cumulative $2.00 spend envelope, if later authorized, grants money authority only and does not release a source, change cadence, or bypass review.

Google Sheets, Layer E semantic acceptance, mapping, reconciliation, and compiled prose remain prohibited.
