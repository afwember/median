# MEDIAN successor-thread cold-start audit v0.8

Paste the following into a new Compile task. It grants no authority.

> Begin a repository-governed cold start from the current local checkout. Confirm root `AGENTS.override.md` is absent, read root `AGENTS.md` completely, and perform its required cold-start sequence exactly. Remain read-only.
>
> Report with active-path citations: Local `HEAD`, `origin/main`, and equality; the Exact corpus vector; Authorial Grammar as the next source; old-plan C0001's calibration-only boundary; accepted new-plan structural C0002 scope and exact result; the complete-source 13-chunk structural plan and 51-group audit; exact cumulative spend and active money-only balance; why the old-plan C0001 result does not accept new-plan C0001; the exact remaining C0001 plus C0003-C0013 sequence and aggregate conservative ceiling; the independent source lifecycle and cumulative spend envelope; Every condition that halts execution; prohibited later stages; the next possible author transition; and the Complete guard result and test count.
>
> If any control conflicts, hash fails, required file is missing, an indivisible semantic group exceeds its calibrated limit, `AGENTS.override.md` exists, or state is ambiguous, stop and report it. Do not modify files, contact a provider, interact with Google Sheets, or begin extraction.

The audit grants no repository-write, provider-call, full-source, or
additional-spend authority. Pilot acceptance does not release the remaining
source.
