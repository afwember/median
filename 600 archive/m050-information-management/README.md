# MEDIAN v0.5.0 information-management archive

This directory preserves the extraction, atomization, authorial triage,
rewrite, MSID mapping, semantic-reconciliation, provider-call, spend,
Worker-lifecycle, and phase-specific regression data and machinery retired
after checkpoint `2745e34`. The former root contract, central guard, and
dashboard renderer remain recoverable at that Git checkpoint.

It is historical evidence, not an active compile system. Its internal paths and
scripts describe the repository layout at that checkpoint and are not expected
to execute after archival relocation. Use Git at `2745e34` when an executable
reconstruction is required.

The active authorial corpus is
`m050/corpus/M050_Authorial_Atom_Corpus_MEDIANv0_5_0.jsonl`. The archive should
be consulted only for a specific provenance or process-history question. It
must not be searched or ingested wholesale during ordinary GDD composition.

Final recorded provider expenditure in the stopped system was `$65.6034299`,
with `$0.8114220` of the last authorization unused. No balance or provider
authority survives into the active authorial process.
