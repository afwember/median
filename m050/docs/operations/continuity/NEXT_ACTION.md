# MEDIAN Next Action

Action date: 2026-08-03

Transition: deterministic replay result to bounded legacy repair queue

## Preconditions

1. confirm the legacy replay checkpoint is committed and pushed;
2. confirm local `HEAD` and `origin/main` are identical and the worktree is
   clean;
3. read the legacy replay milestone report and receipt and active control index
   v0.4;
4. verify all four replay ledgers and reports still pass the standing guard;
5. preserve all 913 legacy inputs and all replay artifacts byte-for-byte;
6. keep all follow-on work offline and source-preserving.

## Authorized transition

Resolve the 24 replay exceptions without changing or silently splitting the
legacy records:

1. reconstruct all 41 Human Rulings sections and labeled fields, including the
   deterministic active-to-legacy reference-rewrite mapping for six records;
2. prepare explicit compound dispositions for the 17 exact quotations that
   cross structural block boundaries;
3. resolve the one repeated MSID quotation using pinned source coordinates or
   place it in human-required state;
4. repeat all four replays and require byte-identical output with no hidden
   grounding failure;
5. only then generate Layer E candidates for records whose mandatory repairs
   and risk requirements are satisfied;
6. build retrospective block and exclusion ledgers for the four legacy sources;
7. build risk-tiered migration bundles, including the broader 123 compound
   review flags;
8. prepare one fresh-source identity card, dry plan, human-effort estimate, and unsent
   calibration request.

## Stop conditions

Stop if a repair requires inventing text, silently splitting a compound record,
choosing among substantively different interpretations, changing a legacy
input, weakening an approved identity boundary, or accepting a quotation that
cannot be grounded. Also stop before any provider request, positive-cost work
order, production mapping, reconciliation, or compilation. Those require
evidence review and, for paid work, new explicit author authorization.
