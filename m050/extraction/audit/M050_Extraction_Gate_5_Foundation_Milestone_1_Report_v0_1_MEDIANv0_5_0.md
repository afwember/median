# MEDIAN v0.5.0 Extraction — Gate 5 Foundation Milestone 1 Report

**Date:** 2026-08-02
**Status:** Passed as an offline implementation milestone; Gate 5 remains in progress
**External model calls:** 0
**Accounted implementation cost:** 0 cents

## Result

The first Gate 5 implementation milestone passes. MEDIAN now has a reproducible, source-independent, offline processing foundation on the dedicated Mac Mini.

This is not a Gate 5 completion report. It does not authorize provider calls, corpus extraction, production mapping, reconciliation, or compilation.

## Implemented controls

- approved technical contract and precise artifact/state lifecycles;
- isolated CPython 3.12 project environment;
- exact arm64 wheel lock with SHA-256 hashes;
- idempotent bootstrap and verification entry point;
- JSON Schema 2020-12 artifact contracts for the complete E/M/R/C chain;
- deterministic JSON serialization, hashing, and content-derived IDs;
- append-only file creation and predecessor-hash receipt chains;
- author-only provider authorization and human-ruling confirmation transitions;
- conservative text normalization with unique raw-span recovery;
- complete structural Markdown blocking and dual token/claim-block chunk limits;
- exact quotation grounding, block-disposition completeness, and low-yield review signals;
- first-class exclusion records, risk tiers, and deterministic sampling;
- dependency/staleness traversal;
- reconciliation bundle membership hashes, exceptional-state accounting, and orphan sweeps;
- fixed-cent cost caps and upward rounding of fractional-cent provider charges;
- frozen input allowlists, hard `m051/` exclusion, and scoped append-only outputs;
- raw response capture and fail-closed JSON/truncation handling;
- narrow offline CLI commands for blocking, planning, work-order verification, proposal validation, and bundle construction;
- versioned Gate 5 guard layered over the immutable Gate 4 guard.

## Verification

- Main Mini environment: 53 tests passed.
- Clean temporary environment: installed only from reviewed local wheel files with package-index access disabled; preflight and all 53 tests passed.
- Runtime lock agreement: 14 of 14 packages matched exact versions.
- Provider SDKs in offline-core lock: 0.
- Provider imports in offline engine: 0.
- Regression catalog: 36 explicit cases, each linked to an executable test.
- Gate 5 engine snapshot: 37 files.
- Gate 5 engine ordered path/hash digest: `d77ae05366d4a5f5ada8a64ba83b73dce3b90fbc14067b1c711ed34e25b3176f`.
- Original Gate 4 guard: pass.
- Frozen source files: 33 unchanged.
- Immutable accepted artifacts: 6 unchanged.
- Sealed archive files: 9,921 unchanged.
- `m051/` input: prohibited.
- Workbook formula-error scan: zero matches.
- Progress workbook visual verification: pass.

The credential preflight inspected only expected path existence and file permissions. Both credentials remained private; their contents were not read.

## Current boundary

Gate 5 remains in progress. The next authorized work is offline only:

1. issue and verify versioned source identity cards;
2. exercise deterministic replay against preserved legacy responses;
3. migrate the 913 grounded legacy records into Layer E candidates without changing legacy artifacts;
4. reconstruct all 41 Human Rulings sections and labeled fields;
5. build retrospective block and exclusion ledgers for the four legacy sources;
6. create and review risk-tiered migration bundles, including the 123 compound flags;
7. prepare one fresh-source identity card, dry plan, human-effort estimate, and unsent calibration request.

No provider request may be sent until Gate 5 has a later completion report and the author separately approves the exact positive-cost work order.
