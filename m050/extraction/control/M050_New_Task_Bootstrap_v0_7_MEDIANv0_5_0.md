# MEDIAN successor-thread cold-start audit v0.7

Paste the following into a new Compile task. It grants no authority.

> Begin a repository-governed cold start from the current local checkout. Confirm root `AGENTS.override.md` is absent, read root `AGENTS.md` completely, and perform its required cold-start sequence exactly. Remain read-only.
>
> Report with active-path citations: Local `HEAD`, `origin/main`, and equality; the Exact corpus vector; Authorial Grammar as the next source; accepted quantized C0001 scope; the rejected old-plan C0002 semantic list-boundary defect; why chunk count is a generated result; the complete-source structural replan at 20 targets; its 13 generated chunks; the 51 audited semantic lead-in/body groups and zero cross-chunk violations; the frozen new-plan C0002 pilot boundary; exact cumulative spend and active money-only balance; the independent source lifecycle and cumulative spend envelope; Every condition that halts execution; prohibited later stages; the exact next possible author transition; and the Complete guard result and test count.
>
> If any control conflicts, hash fails, required file is missing, an indivisible semantic group exceeds its calibrated limit, `AGENTS.override.md` exists, or state is ambiguous, stop and report it. Do not modify files, contact a provider, interact with Google Sheets, or begin extraction.

The audit grants no repository-write, provider-call, full-source, or
additional-spend authority. The offline structural replan is complete; only an
exactly bound one-call C0002 pilot authorization can advance the lifecycle.
