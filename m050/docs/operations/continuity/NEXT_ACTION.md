# MEDIAN Next Action

Action date: 2026-08-03

Transition: Human Rulings reconstruction to cross-block compound disposition

## Preconditions

1. confirm the Human Rulings reconstruction checkpoint is committed and pushed;
2. confirm local `HEAD` and `origin/main` are identical and the worktree is clean;
3. read the Human Rulings reconstruction report and receipt and active control
   index v0.5;
4. verify the 41-section registry, 348-field coverage, 173-record coordinate
   ledger, and six-record reference-rewrite coverage with the standing guard;
5. preserve all legacy inputs, replay artifacts, and reconstruction artifacts
   byte-for-byte;
6. keep all follow-on work offline and source-preserving;
7. keep all Google Sheets interactions paused.

## Authorized transition

Resolve the remaining 18-record repair overlay without changing or silently
splitting any legacy record:

1. enumerate all 17 exact quotations that cross structural block boundaries;
2. bind each quotation to its complete ordered block set and exact contiguous
   source span;
3. assign an explicit compound disposition that preserves the original record
   as one compound evidence item;
4. distinguish safe structural compounds from records needing semantic or
   human review without deciding their meaning by inference;
5. resolve the one repeated MSID quotation using pinned source coordinates or
   place it in human-required state;
6. repeat all four deterministic replays and require byte-identical output with
   no hidden grounding failure;
7. only then generate Layer E candidates for records whose mandatory repairs
   and risk requirements are satisfied;
8. build retrospective block and exclusion ledgers for the four legacy sources;
9. build risk-tiered migration bundles, including the broader 123 compound
   review flags;
10. prepare one fresh-source identity card, dry plan, human-effort estimate, and
    unsent calibration request.

## Stop conditions

Stop if a repair requires inventing text, silently splitting a compound record,
choosing among substantively different interpretations, changing a legacy or
reconstruction artifact, weakening an approved identity boundary, or accepting
a quotation that cannot be grounded. Also stop before any provider request,
positive-cost work order, production mapping, reconciliation, or compilation.
Those require evidence review and, for paid work, new explicit author
authorization.
